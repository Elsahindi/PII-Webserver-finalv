# PII-Webserver-finalv
This is a simple static web server project that allows you to serve static files over HTTP. This web server is written in Python and can be run on any platform that supports Python.
